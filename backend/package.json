{
  "name": "@liveopspoppy/backend",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "description": "LiveOpsPoppy backend — deploys and tears down the stack via AgentsPoppy-brokered credentials; the admin plane for titles, config and stats.",
  "main": "src/server.ts",
  "scripts": {
    "pretypecheck": "npm run gen:backend --prefix ..",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run gen:backend --prefix ..",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.700.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.1107.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-lambda": "^3.700.0",
    "@aws-sdk/client-pricing": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
