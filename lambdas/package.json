{
  "name": "@liveopspoppy/lambdas",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "description": "LiveOpsPoppy collector Lambda — serves remote config and ingests game telemetry.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
